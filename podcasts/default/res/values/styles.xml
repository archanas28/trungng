<!--  default themes (applicable to devices with api < 11) -->
<resources>
    <style name="Theme.Light" parent="@android:style/Theme.Light"/>
    <style name="Theme.Black" parent="@android:style/Theme.Black"/>
    <style name="ButtonBar" parent="android:ButtonBar"/>
    <style name="Theme.Dialog" parent="@android:style/Theme.Dialog"/>
    <style name="Widget.Button.Dialog" parent="@android:style/Widget.Button"/>
</resources>
